{% extends 'base.html.twig' %}

{% block title %}Fiche client{% endblock %}

{% block body %}
    <h1>Fiche client</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ID</th>
                <td>{{ customer.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ customer.lastname }}</td>
            </tr>
            <tr>
                <th>Prénom</th>
                <td>{{ customer.firstname }}</td>
            </tr>
            <tr>
                <th>Addresse</th>
                <td>{{ customer.address1 }}</td>
            </tr>
            <tr>
                <th>Code postal</th>
                <td>{{ customer.zipcode }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ customer.city }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_customer_index') }}">Retour &agrave; la liste</a>

    <a href="{{ path('app_customer_edit', {'id': customer.id}) }}" class="btn btn-outline-success">Modifier</a>

    {{ include('customer/_delete_form.html.twig') }}
{% endblock %}
